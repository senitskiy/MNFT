import React from "react";
import { Paper, Box, Stack, Avatar, Typography, Button } from "@mui/material";

export const CardMNFT = ({ author, sponsor, mnft }) => {
    return(
        <Paper>
            <Box>

            </Box>
        </Paper>
    );
}