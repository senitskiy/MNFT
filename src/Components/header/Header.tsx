import { Stack } from "@mui/material";
import { Box } from "@mui/system";

export const Header = () => {
    return(
        <Box>
            <Stack>
                
            </Stack>
        </Box>
    );
}