import { Container, Grid } from "@mui/material";

const Marketplace = () => {
    return(
        <Grid>
            <span>Marketplace M-NFT</span>
        </Grid>
    );
} 

export default Marketplace;